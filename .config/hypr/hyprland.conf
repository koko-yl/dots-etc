
# See https://wiki.hyprland.org/Configuring/Monitors/

# monitor=,1920x1080,auto,1,bitdepth,8
monitor=,preferred,auto-right,1,bitdepth,8
# monitor = HDMI-A-2, 1920x1080, 1920x0, 1
monitor=eDP-1,preferred,auto,1,mirror,eDP-1,bitdepth,8s
workspace=9,monitor:desc:!
workspace=10,monitor:desc:!

# Optional: fallback rule to ensure other workspaces stay on eDP-1
workspace=1,monitor:eDP-1
workspace=2,monitor:eDP-1
workspace=3,monitor:eDP-1
workspace=4,monitor:eDP-1
workspace=5,monitor:eDP-1
workspace=6,monitor:eDP-1
workspace=7,monitor:eDP-1
workspace=8,monitor:eDP-1
# workspace=11,monitor:"DP-6"
# monitor=,preferred,auto-right,auto

xwayland {
	force_zero_scaling = true
}

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = dolphin
$menu = pidof -x wofi >/dev/null || QT_QPA_PLATFORM=xcb wofi


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = swaybg -i ~/Pictures/wallpapers/wallpaper_g2.png &
exec-once = mpvpaper ALL ~/Pictures/wallpapers/wallpaper_zd2.mp4 --mpv-opt='--hwdec=nvdec-copy --loop --load-scripts=no --mute=yes --panscan=1'
exec-once = ~/flexbar/target/debug/f 
exec-once = hyprpm reload -n
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = GTK_THEME,Catppuccin-Peach-Dark
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1
env = WLR_RENDERER_ALLOW_SOFTWARE,1
env = __GLX_VENDOR_LIBRARY_NAME,mesa

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 3
    gaps_out = 5

    border_size = 3

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(cba6f7cc) rgba(8839efcc) 45deg
    # col.active_border = rgba(ff0000cc) rgba(fff000cc) rgba(00ff00cc) rgba(00fff0cc) rgba(0000ffcc) rgba(ff00ffcc) 45deg
    # col.inactive_border = rgba(ff000033) rgba(fff00033) rgba(00ff0033) rgba(00fff033) rgba(0000ff33) rgba(f0000f33) 45deg
    col.inactive_border = rgba(74c7ecaa) rgba(89b4faaa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 3
#
#     # Change transparency of focused and unfocused windows
#     active_opacity = 1.0
#     inactive_opacity = 1.0
#
#     drop_shadow = true
#     shadow_range = 4
#     shadow_render_power = 3
#     col.shadow = rgba(1a1a1aee)
#
#     # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 2
        vibrancy = 0.1696
        brightness=0.35
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    # bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = myBezier, 0.1, 0.9, 0.1, 1a
    animation = windows, 1, 5, myBezier
    # animation = windowsIn, 1, 2, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 3, default

    bezier = ease, 0.25, 0.1, 0.25, 1.0

    animation = layers, 1, 1, ease, slide
#   animation = layersIn, 1, 30, default, slidefade
#    animation = layersOut, 1, 30, default, slide    
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
        disable_while_typing = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {
#     workspace_swipe = false
# }

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mod, Return, exec, $terminal
bind = $mod_SHIFT, Q, killactive,
bind = $mod, M, exit,
bind = $mod, L, exec, hyprlock
bind = $mod, E, exec, $fileManager
bind = $mod, V, togglefloating,
bind = $mod, D, exec, $menu
bind = $mod, G, exec, ~/launcher/src-tauri/target/release/launcher
bind = $mod_SHIFT, grave, exec, /mnt/vm/start.sh
bind = $mod_SHIFT, backslash, exec, obs-clip
bind = $mod_SHIFT, r, exec, obs-cmd recording toggle
bind = $mod_SHIFT, W, exec, pkill aplay || aplay w.wav 
bind = $mod, P, exec, hyprpicker # dwindle
bind = $mod_SHIFT, P, pin
bind = $mod, J, togglesplit, # dwindle
bind = $mod, F, fullscreen
bind = , Print, exec, komali-screenshot

# Move focus with mod + arrow keys
bind = $mod, left, movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up, movefocus, u
bind = $mod, down, movefocus, d

bind = $mod_SHIFT, left, movewindow, l
bind = $mod_SHIFT, right, movewindow, r
bind = $mod_SHIFT, up, movewindow, u
bind = $mod_SHIFT, down, movewindow, d

# bind = $mod,grave,hyprexpo:expo, toggle
# bindr = $mod,grave,hyprexpo:expo, off
# bind = $mod_SHIFT, left, moveactive, 30 0
# bind = $mod_SHIFT, H, movewindow, -30 0
# bind = $mod_SHIFT, K, movewindow, 0 -30l
# bind = $mod_SHIFT, J, movewindow, 0 30 J

# Switch workspaces with mod + [0-9]
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Move active window to a workspace with mod + SHIFT + [0-9]
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mod, S, togglespecialworkspace, magic
bind = $mod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mod + scroll
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1

# Move/resize windows with mod + LMB/RMB and dragging
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
windowrulev2 = tile,title:(.*RARS 1\.6)
windowrulev2 = suppressevent fullscreen,class:org.eden_emu.eden
windowrulev2 = suppressevent fullscreen,class:info.cemu.Cemu
# windowrulev2 = fullscreen,class:kitty
windowrulev2 = plugin:invertwindow,title:(.*RARS 1\.6)
windowrulev2 = plugin:invertwindow,class:(quartus)
# windowrulev2 = plugin:invertwindow,title:(Quartus Prime*)
windowrulev2 = plugin:invertwindow,title:(.*Maple 202[345])
windowrulev2 = tile,title:(.*Maple 202[34])
windowrulev2 = float,title:(__FLOAT__)
# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,full :0,pinned:0
# windowrulev2 = animation slide left, title:Wofi

# debug:disable_logs = false

# animation = W_ANIM, 1, 8, default
#chromakey_background = 7,8,17
#animation = layers, 1, 40, default, slide left
#layerrule = animation slide, notifications
#bezier = ease, 0.25, 0.1, 0.25, 1.0
#animation = layers, 1, 30, ease, slide, class:wofi
#layerrule = blur,^(wofi)$
# windowrulev2 = noblur, title:(.*Code - OSS)
# windowrulev2 = noblur, class:waterfox

# kill steamwebhelper/steam 'Special Offers'
# windowrulev2 = kill, class:steam title:'Special Offers'
layerrule = noanim, hyprpicker

layerrule = noanim, selection 
exec-once = hyprpm reload
plugin {
    hyprexpo {
        gap_size = 1
        bg_col = rgb(aaaaaa)
        skip_empty = true
    }
}

